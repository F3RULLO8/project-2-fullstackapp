<section class="profile">
  <h2>{{user.local.email}}</h2>
  {{#if user.gifs}}
    <div class="gifs">
      {{#each user.gifs}}
         {{> 'gif'}}
      {{/each}}
    </div>
  {{else}}
    <p>No Gifs yet</p>
  {{/if}}
</section>
